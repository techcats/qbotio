general:
  branches:
    only:
      - dev
deployment:
  production:
    branch: dev
    commands:
      - bower install
      - gulp
      - git config --global ser.email circleci@circleci
      - git config --global user.name CircleCI
      - gulp deploy
test:
  override:
    - echo "test"