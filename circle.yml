general:
  branches:
    only:
      - dev
deployment:
  production:
    branch: dev
    commands:
      - npm install
      - bower install
      - gulp
      - gulp deploy
test:
  override:
    - echo "test"