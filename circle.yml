general:
  branches:
    only:
      - dev
deployment:
  production:
    branch: dev
    commands:
      - bower install
      - gulp
      - git config user.email $GIT_USER_EMAIL
      - git config user.name $GIT_USER_NAME
      - gulp deploy
test:
  override:
    - echo "test"